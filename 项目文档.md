# ESP32-S3 通过 AI 语音识别控制全屋灯光

## 项目背景

传统开关灯需要走到固定位置手动操作，尤其夜间或双手忙碌时极为不便。本项目以 **ESP32-S3** 为核心，结合 **云端 AI 语音识别**（自研端云流水线）实现自然语言控制灯光，提升生活便捷性。

同时，本项目旨在深入学习 **ESP32-S3 与 AI 大模型互联** 的软硬件协同设计，涵盖音频采集、播放、显示、PWM 调光、OTA 升级等全栈功能，为后续分布式智能家居系统奠定基础。

## 项目硬件

### 核心芯片

- **ESP32-S3-N16R8**（Wi-Fi + BLE 5.0，双核 Xtensa LX7，16MB Flash + 8MB PSRAM）

### 外设模块

1. **INMP441** – 数字 PDM 麦克风（语音采集）
2. **NS4168** – I²S 数字功放（语音播报反馈）
3. **0.96 英寸 OLED**（SSD1306 驱动，I²C 接口）– 显示状态信息
4. **2 个HW-478的LED 灯珠** – 三路RGB的PWM 调光控制

### 引脚接线（完整表格）

| 模块名称                   | 模块引脚 | ESP32-S3 引脚 | 说明                                  |
| :------------------------- | :------- | :------------ | :------------------------------------ |
| **INMP441**                | VCC      | 3.3V          | 电源                                  |
|                            | GND      | GND           | 共地                                  |
|                            | SD       | GPIO6         | 数据输出 (PDM_DATA)                   |
|                            | SCK      | GPIO7         | 时钟输入 (PDM_CLK)                    |
|                            | WS       | GND           | 声道选择（接地 = 左声道）             |
|                            | L/R      | GND           | 左/右声道选择（接地 = 左声道）        |
| **NS4168**                 | VCC      | 3.3V          | 电源（注意电流，可接 5V 若支持）      |
|                            | GND      | GND           | 共地                                  |
|                            | BCLK     | GPIO8         | I²S 位时钟                            |
|                            | LRCK     | GPIO9         | I²S 左右声道时钟                      |
|                            | SDIN     | GPIO10        | I²S 数据输入                          |
|                            | MCLK     | 悬空          | 主时钟（NS4168 内部可倍频，无需外接） |
| **OLED (SSD1306)**         | VCC      | 3.3V          | 电源                                  |
|                            | GND      | GND           | 共地                                  |
|                            | SDA      | GPIO11        | I²C 数据线                            |
|                            | SCL      | GPIO12        | I²C 时钟线                            |
| **HW-478模块-LED1-卧室灯** | R        | 1             | R-红色，PWM输入                       |
|                            | G        | 2             | G-绿色，PWM输入                       |
|                            | B        | 20            | B-蓝色，PWM输入                       |
|                            | GND      | GND           | 地                                    |
| **HW-478模块-LED2-客厅灯** | R        | 21            | R-红色，PWM输入                       |
|                            | G        | 17            | G-绿色，PWM输入                       |
|                            | B        | 18            | B-蓝色，PWM输入                       |
|                            | GND      | GND           | 地                                    |

> **注意**：所有模块的 GND 必须共地；INMP441 和 NS4168 的电源建议加 10μF+0.1μF 去耦电容。

## 采用框架

- **ESP-IDF v5.2.6**（官方 SDK，支持 FreeRTOS 及丰富组件）

### esp-idf的sdk配置：

CPU->flash->psram->freertos->partition table->websocket

### 软件架构

- **全双工实时通信**（WebSocket）；
- **MCP 风格工具调用**（云端大模型通过结构化指令控制设备）。

```reStructuredText
project/
├── main/                          # 主程序入口
│   ├── app_main.c
│   └── CMakeLists.txt
├── components/
│   ├── bsp/                       # 板级支持包（硬件抽象层）
│   │   ├── CMakeLists.txt				# CMakeLists.txt
│   │   ├── pwm_driver/                 # PWM 封装（LED调光）
│   │   ├── i2c_driver/                # I²C 封装（OLED）
│   │   ├── i2s_driver/                # I²S 封装（音频输入输出）
│   │   ├── wifi_manager/          		# Wi-Fi 连接/重连，AP配网
│   │   └── audio_codec/           	   # 音频编解码（OPUS 解码）
│   ├── middlewares/               # 中间件（外设驱动）
│   │   ├── CMakeLists.txt				# CMakeLists.txt
│   │   ├── oled/                  		# OLED 显示驱动
│   │   ├── LED_Control/				# HW-478的LED驱动
│   │   ├── microphone/            		# INMP441 驱动（PDM 采集成帧）
│   │   └── amplifier/             		# NS4168 驱动（I²S 音频输出）
│   ├── Audio_Stream/	       	   # 麦克风，功放的流向控制，主要是应对多消费者
│   │   ├── CMakeLists.txt				# CMakeLists.txt
│   ├──App/						   # 应用层
│   │   ├── CMakeLists.txt				# CMakeLists.txt
│   │   ├── ASR/						# ASR唤醒
│   │   ├── AI_Coud/					# 远程大模型通信
│   └── OTA_Update/                # OTA 升级（局域网方案）
└── datasheet/               # 所有模块的参考资料
```

ASR唤醒之后，判断AI_Coud有没有初始化，能不能用，能用再启动它，两者尽量依赖少

## FreeRTOS 任务设计

系统启动后创建以下独立任务（采用事件驱动 + 消息队列通信）：

| 任务名称         | 优先级 | 栈大小    | 功能描述                                                     | 备注                                                         |
| :--------------- | :----- | :-------- | :----------------------------------------------------------- | ------------------------------------------------------------ |
| AI_Cloud任务     | 10     | 8192      | 从Audio_Stream模块的麦克风生产者读取麦克风数据给云端ai       |                                                              |
| ASR语音唤醒任务  | 8      | 32 * 1024 | 也同样作为麦克风的消费者从Audio_Stream模块的麦克风生产者中读取麦克风数据，判断是否触发了语音唤醒词 | 使用xTaskCreateStatic创建任务，因为ASR需要的栈大，防止消耗宝贵的内部SRAM |
| 音频流生产者任务 | 9      | 8 * 1024  | 麦克风音频流生产者任务，有数据就通过任务通知唤醒所有等待数据的消费者 |                                                              |
| 播放任务         | 7      | 8192      | 从各种生产者获得音频数据然后功放出来                         |                                                              |
| 配网任务         | 2      | 4096      | 配网任务，当没有nvs保存的wifi凭证或者当没连nvs保存的wifi的时候就使用ap配网，开启这个任务 |                                                              |
| scan_task        | 3      | 8192      | 扫描wifi的任务，当进入ap配网，网页点击扫描的时候才会有的     | 这个隶属于上一个任务                                         |
| OLED 显示任务    | 4      | 2048      | 显示灯的状态（开/关，颜色，亮度）                            |                                                              |
| app_main         | 默认   | 默认      | 各种初始化                                                   |                                                              |

> **通信机制**：任务间使用 **FreeRTOS 队列** + **事件标志组**。音频流数据块通过Audio_Stream环形缓冲区获取同一份数据。

## Flash 分区表设计（16MB，支持 OTA）

本项目 ESP32-S3-N16R8 配备 **16MB（128Mbit）** 片外 Flash，固件体积会随着音频编解码库（OPUS）、WebSocket 协议、JSON 解析等模块的增加而变大。因此，我们设计了一套 **支持双 OTA（A/B 分区升级）** 的分区表，确保升级失败时能自动回滚。

### 分区表文件 (`partitions.csv`)

在项目根目录下创建 `partitions.csv` 文件，内容如下：

```csv
# ESP-IDF Partition Table for 16MB Flash (ESP32-S3-N16R8)
# Name,        Type,   SubType,   Offset,    Size,       Flags
nvs,            data,   nvs,       0x9000,    0x6000,
phy_init,       data,   phy,       0xF000,    0x1000,
ota_data,       data,   ota,       0x10000,   0x2000,
ota_0,          app,    ota_0,     0x20000,   0x500000,
ota_1,          app,    ota_1,     0x520000,  0x500000,
model,          data,   spiffs,    0xA20000,  0x300000,
storage,        data,   spiffs,       0xD20000,  0x2E0000,
```

### 分区功能详解

| 分区         | 偏移量       | 大小                  | 说明                                                         |
| :----------- | :----------- | :-------------------- | :----------------------------------------------------------- |
| **nvs**      | `0x9000`     | `0x6000` (24 KB)      | NVS 非易失性存储，用于保存用户配置、Wi-Fi 校准、蓝牙配对等关键数据。 |
| **phy_init** | `0xF000`     | `0x1000` (4 KB)       | PHY 初始化数据，包含射频硬件调谐参数，由 ESP-IDF 自动管理。  |
| **ota_data** | **0x10000**  | **0x2000 (8 KB)**     | ✅ **OTA 状态分区（强制要求）**，记录当前及升级中的固件分区信息。 |
| *(空隙)*     | *0x12000*    | *0xE000 (56 KB)*      | 📍 未使用空间，因 ota_0 需从 `0x20000` 起始以满足 64KB 对齐。 |
| **ota_0**    | **0x20000**  | **0x500000 (5 MB)**   | ✅ 应用程序固件分区 A，起始地址对齐至 64KB，用于 OTA 升级。   |
| **ota_1**    | **0x520000** | **0x500000 (5 MB)**   | ✅ 应用程序固件分区 B，与 ota_0 大小一致，支持双区 OTA 回滚。 |
| **model**    | `0xA20000`   | `0x300000` (3 MB)     | SPIFFS 文件系统分区，用于存储 AI 模型，存储esp-sr组件的模型  |
| **storage**  | `0xD20000`   | `0x2E0000` (~2.88 MB) | spiffs文件系统分区，可用于存储用户日志、配置文件或可写数据，**重要的是存储了websocket_server的html网站** |

## OTA 固件升级功能实现（局域网方案）

本项目采用 **“开机自动检测，拉取式升级（Pull-style OTA）”** 的局域网方案。通过在局域网内部署一个轻量级的 Python 版本管理服务器，实现设备上电后自动比对版本并完成固件更新，无需人工干预或通过浏览器手动触发。

### 1. 升级流程架构

系统采用“检测-对比-执行”的闭环逻辑。

```codeReStructuredText
┌──────────────────┐           1. 获取版本信息 (JSON)        ┌──────────────────┐
│   ESP32-S3 终端   │ ───────────────────────────────────▶ │  Python 后端服务器 │
│ (192.168.4.xxx)  │ ◀─────────────────────────────────── │  (192.168.4.16) │
└────────┬─────────┘        2. 返回最新版本号及下载 URL       └────────┬─────────┘
         │                                                         │
         │ 3. 版本比对 (Local vs Remote)                           │
         │    - 若 Local < Remote: 进入步骤 4                      │
         │    - 若一致: 进入主程序                                 │
         │                                                         │
┌────────▼─────────┐           4. 请求固件文件 (.bin)        ┌────────▼─────────┐
│    OTA 任务启动   │ ───────────────────────────────────▶ │   固件存储目录    │
│ (写入备选 App 分区)│ ◀─────────────────────────────────── │ (esp_v1.0.1.bin) │
└────────┬─────────┘           5. 下载并校验数据流           └──────────────────┘
         │
         │ 6. 更新启动分区并重启
         ▼
      [重启运行新固件]
```

### 2. 后端服务器设计 (Python)

后端由 Python 的 Web 框架（如 Flask 或 FastAPI）实现，提供两个核心能力：

- **配置中心 (Metadata API)**：
  提供一个固定的 HTTP 接口（如 /ota/check），返回当前最新的固件版本号、文件名及可选的校验和（MD5/SHA256）。该信息通过 JSON 格式交互，便于扩展（如强制升级标志、更新日志等）。
- **文件托管 (Static Files)**：
  托管编译生成的二进制固件文件（.bin）。服务器需支持大文件流式传输，确保 ESP32 在下载过程中内存占用最小化。

### 3. 客户端逻辑实现 (ESP32-S3)

#### 3.1 版本管理策略

- **硬编码版本号**：在固件代码中使用宏定义标记当前版本（如 PROJECT_VERSION "1.0.0"）。
- **元数据解析**：设备连接 Wi-Fi 后，首先访问后端 API。解析 JSON 获取远程版本字符串，并将其转化为数字逻辑进行比对。

#### 3.2 升级序列流程

1. **初始化检测**：网络就绪后，OTA 检查任务获得最高优先级。
2. **双分区切换**：根据 ota_data 分区的状态，ESP-IDF 的 OTA 组件会自动识别当前的活动分区（如 ota_0），并将下载的数据流顺序写入非活动分区（如 ota_1）。
3. **流式写入**：采用边下载、边写入、边校验的方式，不占用大量的 PSRAM 或 Flash 暂存空间。
4. **确认与回滚**：
   - 下载完成后，系统会自动校验固件签名的完整性。
   - **回滚保护**：新固件首次启动后，必须在成功连接 Wi-Fi 并确认运行正常后，向 ota_data 分区发送“确认（App Valid）”指令。若新固件启动后发生崩溃，系统再次重启时将自动切换回旧版本分区，防止设备因固件缺陷导致无法使用。

### 4. 关键配置与要求

- **网络环境**：ESP32 与 Python 服务器必须处于同一局域网（同一网段）。
- **分区表支撑**：必须配合 partitions.csv 中的双 app 分区设计，每个分区大小需一致且足以容纳完整的固件文件。
- **协议放行**：在 ESP-IDF 配置中，需启用“允许 HTTP OTA（Allow HTTP for OTA）”，因为局域网环境通常不部署复杂的 SSL 证书。
- **并发控制**：在 OTA 升级期间，必须暂停音频采集（I2S）和 WebSocket 通信等高内存/高带宽占用任务，确保升级过程的系统稳定性。

### 5. 运维操作流程

1. **编译**：开发者修改代码后，通过 idf.py build 生成新的 .bin 文件。
2. **发布**：将 .bin 文件放置在服务器固件目录下，并修改 Python 脚本中的版本号变量。
3. **自动部署**：现场所有 ESP32 设备在下次上电或例行心跳检查时，会自动感知更新并完成全量部署。

### 6. 安全与回滚机制（局域网优化）

- **无需 HTTPS 证书**：局域网环境安全可信，直接使用 HTTP 协议，节省证书开销。
- **固件校验（推荐）**：在固件文件末尾附加 CRC32 或 MD5 校验值，ESP32 下载完成后校验，防止文件损坏。
- **自动回滚**：新固件启动后若 5 分钟内未上报心跳，系统自动回退至旧分区。

**校验值实现（简化）**：

```c
// 在固件下载完成后，计算下载数据的SHA256，与固件中预埋的值对比
// 注：更简单的方式是在HTTP服务器上放一个同名的 .sha256 文件
// ESP32下载 firmware.bin 和 firmware.bin.sha256，比对后决定是否烧录
```

### 8. 编译与烧录配置

在 ESP-IDF 中启用分区表和 OTA 功能：推荐直接手动配置，vs code上图形化配置

```bash
# 1. 进入项目配置
idf.py menuconfig

# 2. 配置分区表
#    -> Partition Table -> Custom partition table CSV
#    -> Partition Table File -> 输入 "partitions.csv"

# 3. 配置 HTTP 服务
#    -> Component config -> HTTP Server 按需调整栈大小

# 4. 首次烧录（烧入 ota_0 分区）
idf.py -p /dev/ttyUSB0 flash

# 5. 编译生成固件（用于OTA）
idf.py build
# 固件位置: build/项目名.bin
```

## 项目前景

基于自研端云架构，本方案具备极大扩展性：

1. **私有化部署**：将云端网关部署到内网服务器，实现全本地 AI 控制，不依赖公网。
2. **多模态扩展**：在协议中增加图像帧类型，配合 ESP32-S3 的摄像头接口，实现“语音+视觉”多模态控制。
3. **多设备协同**：在 MCP 指令中增加 `target_device` 字段，通过局域网广播或 Mesh 网络转发指令，实现全屋多芯片联动。
4. **自定义唤醒词**：参考小智离线唤醒方案，使用 ESP-SR 框架增加本地唤醒，只在唤醒后才建立 WebSocket 连接，大幅降低云端费用。
5. **协议开放**：后续可将该二进制协议开源，使其他开发者也能快速接入自己搭建的云端 AI 服务。
6. **局域网 OTA 增强**：后续可开发手机 APP/微信小程序，一键扫描局域网内设备并批量升级固件。

------

*所有硬件模块的详细数据手册、原理图及应用笔记均放置在项目根目录下的 datasheet/ 文件夹中，供开发者查阅。*