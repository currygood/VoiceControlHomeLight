ASR唤醒之后，会触发AiCloud_Start()函数，开始与云端ai进行实时语音聊天。
AiCloud模块与云端ai连接上后会开始倒计时，30s后会自动断开连接，然后调用ASR模块的函数通知ASR模块重新开始检测唤醒词。

""前面提到的还不太好""

目前这样：
ASR唤醒之后，会触发AiCloud_Start()函数，开始与云端ai进行实时语音聊天。
ASR这里就开始30s的倒计时，30s后会通过调用AiCloud_Stop()函数，断开与云端ai的连接。
这样就只是ASR调用AI_Coud模块的函数，不是像上面那样互相调用，这样的架构不太好。
